export type Skill = {
  name: string;
  value: number;
};

export type BossFight = {
  title: string;
  description: string;
};

export type SideQuest = {
  title: string;
  stack?: string;
  goal?: string;
};
